{
  "name": "botcast",
  "private": true,
  "workspaces": [
    "frontend",
    "cms-dashboard",
    "spec",
    "mcp"
  ],
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "npx @biomejs/biome check --write"
    ],
    "cms-dashboard/src/**/*.{ts,tsx}": [
      "npx @biomejs/biome check --write"
    ]
  }
}
